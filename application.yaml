management:
    endpoint:
        gateway:
            enabled: true
    endpoints:
        web:
            exposure:
                include: "*"

logging.pattern.console: '%C{1.} [%-5level] %d{HH:mm:ss} - %msg%n'
